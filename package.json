{
  "name": "benchwarmer",
  "version": "0.1.0",
  "private": false,
  "description": "Rehabilitate benchmark charts into auditable, decision-first layouts.",
  "type": "module",
  "bin": { "benchwarmer": "./bin/benchwarmer.mjs" },
  "scripts": {
    "test": "node --test",
    "check": "node --check app.js && node --check bin/benchwarmer.mjs && node --check worker.js",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "engines": { "node": ">=20" }
}
